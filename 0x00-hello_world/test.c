#include <stdio.h>
/**
 * main - Entry point
 *
 * Print - Size of various datatype of computer it is compiled on
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (a = 0; a < b; a++)
		printf("%d\n", a);

	return (0);
}
